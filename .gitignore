.dotter/local.toml
.dotter/cache.toml
.dotter/cache

# do not track the lazy-lock since it's gets autogenerated on each machine
# we remove it from git instead of not tracking it as it's easier to symlink the whole nvim directory
nvim/lazy-lock.json
